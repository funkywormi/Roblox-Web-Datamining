/**
 * Starts the flow that sets an on-device parent PIN.
 */
const useStartOnDeviceParentLinking = (): (() => void) => {
  return () => undefined; // TODO FAMEX-133: Fill this in
};

export default useStartOnDeviceParentLinking;
