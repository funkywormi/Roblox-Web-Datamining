/**
 * Opens PIN management for the on-device parent.
 */
const useManageParentPin = (): (() => void) => {
  return () => undefined; // TODO FAMEX-133: Fill this in
};

export default useManageParentPin;
