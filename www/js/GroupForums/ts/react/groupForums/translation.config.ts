import type { TranslationConfig } from 'react-utilities';

export const groupsConfig: TranslationConfig = {
  common: ['Feature.FileUploadComponent'],
  feature: 'Feature.Groups'
};

export default { groupsConfig };
