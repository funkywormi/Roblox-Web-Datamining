import React from "react";

const AboutSectionHeader: React.FC = ({ children }) => (
  <span className="group-description-dialog-body-header text-heading-small block">{children}</span>
);

export default AboutSectionHeader;
