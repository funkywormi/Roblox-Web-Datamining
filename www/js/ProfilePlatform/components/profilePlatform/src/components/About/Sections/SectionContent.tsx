import React from "react";

const AboutSectionContent: React.FC = ({ children }) => (
  <span className="group-description-dialog-body-content content-default text-body-medium block">
    {children}
  </span>
);

export default AboutSectionContent;
