import groupModule from '../groupModule';

function groupForumsDiscoveryController($scope) {
  'ngInject';

  const ctrl = this;

  const init = function () {};

  ctrl.$onInit = init;
}

groupModule.controller('groupForumsDiscoveryController', groupForumsDiscoveryController);
export default groupForumsDiscoveryController;
