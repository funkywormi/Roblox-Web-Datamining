import { TranslationConfig } from 'react-utilities';

const groupAffiliatesConfig: TranslationConfig = {
  common: ['CommonUI.Controls'],
  feature: 'Feature.Groups'
};

export default groupAffiliatesConfig;
