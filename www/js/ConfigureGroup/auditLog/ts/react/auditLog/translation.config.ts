import { TranslationConfig } from 'react-utilities';

const auditLogConfig: TranslationConfig = {
  common: ['CommonUI.Controls', 'Feature.GroupManagement'],
  feature: 'Feature.Groups'
};

export default auditLogConfig;
