/**
 * Skeleton placeholder for the title bar, kept separate from the content skeleton so it can be
 * rendered in the dialog's title slot while loading.
 */
const DialogTitleSkeleton = () => {
  return <div className="bg-shift-100 height-[40px] width-[300px] max-width-full radius-medium" />;
};

export default DialogTitleSkeleton;
