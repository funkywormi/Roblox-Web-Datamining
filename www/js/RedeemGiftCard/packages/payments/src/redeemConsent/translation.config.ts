import { TranslationConfig } from "react-utilities";

const redeemGiftCardTranslationConfig: TranslationConfig = {
  common: [],
  feature: "Purchasing.RedeemGameCard",
};

export default redeemGiftCardTranslationConfig;
