import type { FC } from "react";

const BackdropTexture: FC = () => {
  return (
    <div className="backdrop-texture width-full height-[210px] pointer-events-none absolute" />
  );
};

export default BackdropTexture;
